const [a, b, c, d] = [3, 5, 1, 15];

//Operadores binarios (opera com dois operandos)
const soma = a + b + c + d;
const subtracao = d - b;
const multiplicacao = a * b;
const divisao = d / a;
const modulo = a % 2;

console.log(soma, subtracao, multiplicacao, divisao, modulo);

//prefix => ++a
//infix => a + b
//posfix => a++
//unario => -a
